PySide6>=6.7
lxml>=5.0
striprtf>=0.0.29
pyenchant>=3.2

# Text-to-speech (KittenTTS). 0.1.0 is the last release without the
# misaki/spacy dependency chain, which is capped at Python <3.13.
# phonemizer-fork + espeakng-loader supply the espeak-ng backend without
# needing a system espeak-ng install.
kittentts==0.1.0
phonemizer-fork>=3.3
espeakng-loader>=0.2
soundfile>=0.12
